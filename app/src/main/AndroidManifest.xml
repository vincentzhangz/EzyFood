<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vincentzhangz.ezyfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EzyFood">
        <activity android:name=".SnacksActivity"></activity>
        <activity android:name=".FoodsActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".OrderDetail" />
        <activity android:name=".MyOrderActivity" />
        <activity android:name=".DrinksActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>